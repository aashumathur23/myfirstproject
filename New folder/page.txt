this is page which is in desktop > myfirstproject > new folder 

git status

git add [ 
git log

git diff
git diff -cached

git push origin projectname
 
git commit -m projectvversion

git checkout "version id" "filename"

git restore --staged "filename"
git restore "filename"

 git restore --worktree header.html



this has just git command 



this is first change 
